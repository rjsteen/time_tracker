<h2>Edit Task</h2>

<%= render "form.html", Map.put(assigns, :action, project_task_path(@conn, :update, @task, @project)) %>

<span><%= link "Back", to: project_task_path(@conn, :index, @project) %></span>
